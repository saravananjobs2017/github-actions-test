name: Jira Webhook Trigger

on:
  repository_dispatch:
    types: do-something
  workflow_dispatch:

jobs:
  jira_trigger_job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Perform a Task Based on Jira Trigger
        run: echo "Triggered by Jira webhook"


    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Run a one-line script
        run: echo "Hello, ${{ github.event.inputs.environment }}!"
